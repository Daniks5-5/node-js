const userName = "Ivan";

const sayHi = (userName) => `Hello! My name is ${userName}`;

console.log(sayHi(userName));

const userFamily = "Sidorov";

const sayFam = (userFamily) => `My lastname is ${userFamily}`;

console.log(sayFam(userFamily));